# For local dev:
# BASE_URL=http://localhost:3030

# Where to send players after first scan (keep {token}):
GAME_URL_TEMPLATE=https://flashka.onrender.com/?token={token}

# Protect stats pages (optional):
ADMIN_KEY=PickSomethingSecret123

# For Render Postgres persistence (recommended in production):
# DATABASE_URL=postgres://<user>:<pass>@<host>/<db>
